<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.wines.mapper.ProfessionalRatingsMapper">

  <select id="selectRatingByWineTitle" resultType="professionalRatings">
    select * from ProfessionalRatings where wineTitle=#{wineTitle} limit 1
  </select>

</mapper>